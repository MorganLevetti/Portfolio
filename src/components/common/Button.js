import { IconButton } from "@mui/material"

export const Button = ({}) => {
const isClick = [click, set]
  return (
    <IconButton 
    color="primary"
    size="medium"
    onClick={() => {console.log('click')
    }}>
      Click
    </IconButton>
  )
}
